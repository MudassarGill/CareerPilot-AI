# ============================================================
# CareerPilot AI — Backend Dependencies
# ============================================================
# This file lists all Python packages required by the FastAPI
# backend server. Install with:
#
#   pip install -r requirements.txt
#
# Categories:
#   - Web Framework (FastAPI, Uvicorn)
#   - Database ORM (SQLAlchemy, Alembic)
#   - AI / LLM (LangChain, LangGraph, Google GenAI)
#   - RAG Pipeline (ChromaDB, sentence-transformers)
#   - Resume Parsing (PyMuPDF, spaCy)
#   - Speech (Whisper, gTTS)
#   - Auth & Security (python-jose, passlib, bcrypt)
#   - Utilities (python-dotenv, httpx, redis)
# ============================================================

# ---- Web Framework ----
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# ---- Database ----
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
asyncpg==0.29.0

# ---- AI / LLM ----
langchain==0.3.0
langchain-google-genai==2.0.0
langgraph==0.2.0
google-generativeai==0.8.0

# ---- RAG / Embeddings ----
chromadb==0.5.0
sentence-transformers==3.0.0

# ---- Resume Parsing ----
PyMuPDF==1.24.0
pdfplumber==0.11.0
python-docx==1.1.0
spacy==3.7.0

# ---- Speech ----
openai-whisper==20231117
gtts==2.5.0

# ---- Auth & Security ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# ---- Utilities ----
python-dotenv==1.0.1
httpx==0.27.0
redis==5.0.0
pydantic==2.9.0
pydantic-settings==2.5.0
